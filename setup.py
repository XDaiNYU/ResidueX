from setuptools import setup, find_packages

setup(
    name="ResidueX",          # Package Name
    version="0.1",            # Version Number
    packages=find_packages(), # Automatically finds all packages (like ResidueX)
    install_requires=[],      # Leave empty to avoid installing additional dependencies
)

